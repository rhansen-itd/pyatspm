# Roadmap

Planned and deferred work only. The original backlog — a few months of Jules's accumulated review-only suggestions, triaged against the live code — was split into self-contained sessions (A–F, plus the Video Overlay build and audit) and worked through. That history lives in git; the decisions those sessions settled are reflected in [architecture.md](architecture.md) and the test suite. What remains below is the open fix candidates, one deferred decision, a future feature, and a standing "don't re-investigate" list.

## Open fix candidates

Small and actionable; each carries enough file/line detail to be picked up cold.

- ~~**Event timestamps are 0–1.0 s early: the decoder base ignores the header's sub-minute offset.**~~ **Done** — `parse_datz_bytes` now parses `Controller Data Log Beginning` and adds its seconds-plus-fraction to the event base; `utc_start` is untouched, so `ingestion_log` spans, duration inference, gap detection and cycle ranges still anchor to the filename boundary. A new pure `parse_datz_header()` (exported from `analysis`) lets the shell cross-check the header's date and HH:MM against the filename; `_check_header_alignment` warns per file and the count surfaces as `header_mismatches` in the ingest summary. The no-header path falls back to the supplied base. Re-verified on the corpus while landing it: **0 of 14,124 files** lack a header or disagree with their filename, and the worst shifted span on the 1-minute site is 59.9 s — 1.1 s under the 61 s Grid-Mode threshold, as predicted. Covered by `tests/analysis/test_decoders.py` (offset arithmetic, fallbacks, out-of-range rejection) and `tests/data/test_ingestion_header.py` (stored timestamps, span anchoring, cross-file spacing, mismatch warning). **Backfill decision settled at the same time: option (b), rebuild from `raw_data/`.** Plain re-ingestion cannot fix an existing DB — `UNIQUE(timestamp, event_code, parameter) ON CONFLICT IGNORE` (`data/manager.py:119`) plus `INSERT OR IGNORE` (`manager.py:665`) would add shifted rows *alongside* the originals, and `_is_covered` (`ingestion.py:265`) blocks the re-read anyway. So `process` gained Path C: `--rebuild` clears `events`/`cycles`/`ingestion_log` via the new `DatabaseManager.clear_ingested_data()` and re-ingests everything from scratch; `config`/`metadata` survive (they come from `int_cfg.csv` and `metadata.json`), and `cycles` re-derives for free since `cycle_start` is computed, not stored independently. Mutually exclusive with `--fill-gaps`, full `--target`/`--targetid`/`--all` parity, and destructive so it confirms once per run — `--yes` to skip, and it refuses outright on non-interactive stdin without it. Rejected (a) apply-forward-only, which leaves a permanent mixed-basis discontinuity, and (c) an in-place timestamp migration, which still has to re-read every raw file to recover each delta and so gains nothing over (b). Verified end-to-end on a scratch intersection seeded with real `.datZ` files: 868 events deliberately corrupted to the pre-fix basis rebuilt back to 868 (not 1,736 — no duplicate accumulation), stored timestamps matched `boundary + header_offset + binary_offset` exactly on all 7 files with events, cycles regenerated, config/metadata intact; also exercised at scale on 12,490 files / 2.69 M events. Covered by `tests/data/test_manager_rebuild.py` and `tests/test_cli_rebuild.py`. **Running it against the production DBs is still outstanding** — tracked as its own operational item below.

  <details><summary>Original entry</summary>

  `.datZ` time offsets are measured from the *header* instant (`Controller Data Log Beginning:,<M/D/YYYY>,<HH:MM:SS.s>`), which sits 0.0–1.0 s past the filename's clock boundary. `_parse_filename_timestamp` (`data/ingestion.py:283`) yields only the boundary, and that is what reaches `parse_datz_bytes` (`ingestion.py:479`) as `base_timestamp` (`analysis/decoders.py:143`), so every stored event is early by that offset — mean 0.52 s, and a full 1.0 s on 38% of files. Within-file durations are unaffected (uniform shift cancels); **cross-file-boundary durations are wrong by `delta_prev − delta_next`, up to ±1.0 s**, which hits detector occupancy and any phase interval spanning a file edge. Verified two independent ways over the 14,124-file corpus: the offset cap tracks the header offset exactly (`cap = nominal_ds − 1 − 10·delta`, never exceeded), and 6,495 of 6,503 cross-boundary yellow/red-clearance intervals show a residual equal to `delta_prev − delta_next` (the 8 misses are site-315 phases on alternate timing plans; zero exceptions on either 15-minute site). Fix: parse the header line in `parse_datz_bytes` and add its seconds-plus-fraction to the base. Take only the *delta*, not the absolute header time — it needs no timezone, so the functional core stays pure. Two constraints for whoever picks this up: (1) **do not shift `utc_start` itself** — it is the grid anchor for `ingestion_log` spans, `_calculate_duration`/`_grid_duration` (`ingestion.py:512`), gap detection, and cycle-processing ranges, all of which legitimately want the boundary; only the event base moves. (2) The no-header path must stay a supported fallback — `_make_datz` in `tests/analysis/test_decoders.py:40` builds buffers with no `Controller Data Log Beginning` line, so falling back to the supplied base keeps every existing decoder test green. Worth exposing a `parse_datz_header()` helper so the shell can cross-check header `HH:MM` against the filename and warn on disagreement; that check is also a free off-grid-file detector. Checked: the ≤1.0 s shift cannot flip the 61 s Grid-Mode heuristic (worst case 58.9 s → 59.9 s, 1.1 s headroom). Small, Sonnet-sized. Gated on the backfill decision below only for already-ingested DBs — the code fix stands alone.

  </details>
- ~~**Out-of-order timestamps from a backward clock set are ingested silently.**~~ **Done** — `_fence_clock_steps` (`data/ingestion.py`) runs in the shell right after `parse_datz_bytes` returns, while the rows are still in file order, and inserts an `event_code = -1` marker per backward step via the existing `insert_gap_marker`, so every duration/pairing consumer stops at the break per CLAUDE.md §5. Detection is `np.diff(timestamps) < 0` within the file, plus a cross-file check for a break that straddles an edge. **Marker placement settled at 0.05 s below the first post-step event** (`_CLOCK_STEP_MARKER_LEAD`): binary offsets are whole deciseconds off a common per-file base, so half a decisecond can never land on a real event, always sorts immediately ahead of the one it fences, and avoids the collision the existing `prev + 0.1` gap placement would hit inside a replayed band. Deliberately **one marker per step, at the band start, not two** — post-step events are internally consistent and correctly ordered among themselves, so a second marker at the band end would break a genuinely valid pair; only the pre/post interleaving needs fencing. The count surfaces as `clock_steps` in `get_ingestion_stats()` and as `Clock steps back` in the ingest summary, kept separate from `gap_markers` (which stays the database-wide `event_code = -1` total, these included) so clock sets don't blend into comms-gap noise. The cross-file check is guarded on the *previous file's boundary* rather than its last event, because in Gap Fill the last file processed can legitimately sit later on the clock — an unguarded comparison flags ordinary backfill; note also that a well-formed file's offsets never reach the next boundary, so in practice that arm fires on spill-over from an anomalous predecessor (off-grid header, edge-landing set) rather than on a clean straddle. Verified with the shipped detector over the full 14,124-file / 6.72 M-event corpus: **0 files flagged**, matching the predicted zero-false-positive rate. Verified end-to-end on real `.datZ` files with a deliberate −5 s set injected mid-file (offsets replaying a 5.0 s band, mirroring the hardware case): the step was caught and warned about, the marker landed 0.05 s below the first post-step event with **0 collisions** against real rows, and all 1,075 events were still stored — fencing loses no data. Covered by `tests/data/test_ingestion_clock_steps.py` (marker value and sort position, multiple steps, warning text, cross-file edge, Gap-Fill backfill not flagged, comms gap not miscounted, chronological/single-event/empty files silent). Still does **not** restore order or rescue rows lost to `UNIQUE(timestamp, event_code, parameter)` — both belong to the future item below.

  <details><summary>Original entry</summary>

  A backward controller clock adjustment makes the recorded time *decrease* part-way through a file: the offsets replay a band, so events from two different real moments carry the same labels and, sorted by time, land in the wrong sequence. Confirmed on hardware 2026-07-29 — a deliberate −5 s set on 10.70.10.51 produced offsets `310 → 262` inside `ECON_10.70.10.51_2026_07_29_1630.datZ`, replaying a 4.8 s band. Simulated against the real corpus, a 5 s backward set puts **~12 events (site 201) to ~21 events (site 315) out of order**, and yields impossible sequences — a detector actuating while already active, an off with no on. Nothing downstream expects this: `_build_detector_df`/`_build_signal_df` sort by timestamp then pair adjacent rows, and `merge_asof` requires sorted keys. Fix: after `parse_datz_bytes` returns, check for a decreasing timestamp (`df['timestamp'].diff() < 0`) and, where found, insert an `event_code = -1` marker via the existing `insert_gap_marker` so every duration/pairing consumer stops there per CLAUDE.md §5. Do the check in the shell (`_process_file`, `data/ingestion.py:447`) rather than the decoder — it needs no signature change and no new public export, and marker insertion already lives there. Also compare each file's first timestamp against the previous file's last, to catch a set that straddles a file boundary. Detection is essentially free of false positives: **zero backward steps in all 14,124 corpus files** of normal operation, because the logger appends chronologically and time cannot otherwise go backwards. Two things this deliberately does *not* do, both belonging to the future item below: it does not restore the correct order (the marker only prevents measuring *across* the break), and it does not rescue rows lost to `UNIQUE(timestamp, event_code, parameter)` — measured at ~0.1–0.2 events per backward set, ~1% of affected events. Worth surfacing the count separately from ordinary gap markers in the ingest summary so these are visible rather than blending into comms-gap noise. One judgement call for whoever picks this up: the marker timestamp. Existing markers use `prev_last_event_ts + 0.1`, but inside a replayed band that value can collide with a real event, so pick the placement deliberately. Tests are easy — the binary format takes any `uint16` offset, so a synthetic buffer with decreasing offsets exercises it directly. Small, Sonnet-sized.

  </details>
- ~~**`resolve_stopbar_target` has no phase-number range check.**~~ **Done** — it now rejects any integer outside `MIN_PHASE_NUMBER`-`MAX_PHASE_NUMBER` (1-16) with a distinct `"out of range"` `ValueError`, separate from the existing `"Unrecognised stopbar phase field"` raised for non-numeric, non-overlap input. **Bound to 16 deliberately, not to a per-intersection config lookup**: it matches `OVERLAP_LETTER_MAP`'s A-P, which comes from the same Hi-Res Enumerations spec, and keeping the check pure preserves `resolve_stopbar_target`'s use inside `ShapeConfig.relevant_phases()`/`relevant_overlaps()` with no DB handle. A test asserts the two ranges stay equal so they can't drift apart. The 1-16 bounds were already hardcoded in four places in the calibration GUI (`video/calibrate.py`), plus a duplicate `"A" <= x <= "P"` overlap-letter test — all five now import the shared constants / test membership in `OVERLAP_LETTER_MAP` per CLAUDE.md §5, and the dialog prompts derive their range text rather than spelling it out. No stored shape CSV was affected: the corpus has no `*_shapes.csv` with an out-of-range phase (none exist yet), so nothing that previously loaded now fails. Covered by boundary/out-of-range cases in `tests/data/test_video_shapes.py` (the old `test_phase_number_is_not_range_checked`, which pinned the looseness, is replaced); full suite green at 313 passed.
- **Operational: rebuild the production DBs onto the corrected timestamp basis.** The decision is settled and the tooling exists (see the completed item above) — what remains is running it. `atspm process --rebuild --all` re-derives every `events` row from `raw_data/`, so any DB ingested before the header-offset fix still holds rows 0–1.0 s early until it is run. Only worth doing where `raw_data/` still holds the full history; anything already pruned rebuilds to whatever files survive, so check coverage per intersection first. Not urgent — the error is sub-second and uniform within a file — but until it happens each of those DBs is mixed-basis at the cutover point.
- **Fable edge-case pass on the SQLite fixture smoke tests.** `tests/data/test_manager.py` and `tests/data/test_reader.py` carry happy-path smoke tests plus `# TODO(fable):` stubs for the real edge cases: `get_metadata` falling back to `{"timezone": "US/Mountain"}` when the `metadata` table is *missing* vs. *present-but-empty* (two distinct code paths in `manager.py`), and `check_data_quality` completeness scoring around gap markers (marker on the window boundary, `gap_count > event_count` flooring at 0.0, the zero-event-count denominator guard). Adversarial edge-case reasoning → a Fable lane, carrying the standing `event_code == -1` gap-marker audit mandate (CLAUDE.md §5).

## Deferred — needs a project-wide decision first

- **`get_phase_splits()`'s 10-keyword-argument signature (`data/phases.py:423`).** Real, but *every* Engine's `get_X` convenience wrapper has a similarly long explicit kwarg list by convention (`get_vehicle_counts`, `get_arrival_on_green`, …). Fixing this one in isolation would make it inconsistent with its siblings. If worth doing, decide on a project-wide convention (e.g. a shared options dataclass) first, then apply everywhere at once — don't one-off it.

## Future features

- **Correcting for controller clock adjustments, informed by a marker pulse.** The near-term fix above only *fences off* a backward clock set. This is the ambition of actually repairing the affected window, using a known-length detector pulse that `eos_set_time.py` fires across every adjustment it makes (see `econ_itd_tools/ROADMAP.md` — the pulse is the upstream half of this work and has to land first).

  What the experiment established about the mechanism (2026-07-29, hardware-verified): recorded offsets track the *controller clock*, not elapsed real time. A file's clock window is always exactly nominal, but its real duration flexes — the −5 s set made a 1-minute file cover 65 real seconds, the +5 s set made one cover 55. So a backward set duplicates a band of labels and a forward set skips one. No events are ever lost or invented; only the labels are wrong.

  How the pulse recovers the correction: fire a pulse of known length `L` spanning the commit. Recorded length comes back `L + delta` for a forward set and `L − delta` for a backward one (negative if `delta > L`), so one interval yields magnitude, sign, and position at once. That works even when nothing else is happening, which matters because ~10% of empty forward-step holes still have a detector actuation spanning them whose duration silently gains `delta`.

  **Repair by re-timestamping, not reordering.** The events are in the correct sequence in the file — the logger appends chronologically. Only the labels are corrupt. So the repair is to add `delta` back to every event after the break, which restores both order and correct durations. Reordering by timestamp would be the wrong move; it treats the corrupted labels as authority.

  **The gating decision, and it is genuinely project-wide.** Correcting one file's tail by `+delta` makes it overlap the *next* file's head, because that file's labels start at the clock boundary. So re-timestamping alone does not close the problem, it relocates it. Three ways out:
  - (a) keep storing **controller-clock** timestamps and mark discontinuities (what the near-term fix does). Simple, honest, self-consistent per file; durations across a break stay wrong and the DB timeline is not real time.
  - (b) store a reconstructed **real-time** axis by accumulating every correction ever applied. Complete and makes durations correct everywhere, but it changes the meaning of every timestamp in the database, requires a gapless history of corrections (a single missed adjustment silently skews everything after it), and interacts with the `.datZ` base-time fix above — both change stored timestamps, so sequence them deliberately rather than shipping both blind.
  - (c) **re-timestamp, then normalise back into the file's own clock window** — add `delta` to post-break events to restore order and relative spacing, then scale the file's internal timeline by `nominal / (nominal + delta)` so it still ends where the next file begins. This keeps every file self-contained and local, needs no correction history and no downstream shifting, and fixes the ordering problem outright. The price is a known, bounded, uniform error on *every* duration in that one file:

    | file length | delta | scale | error on every duration | a 10.0 s green stores as |
    |---|---|---|---|---|
    | 1-min | 1 s | 0.98361 | 1.64% | 9.836 s |
    | 1-min | 2 s | 0.96774 | 3.23% | 9.677 s |
    | 1-min | 5 s | 0.92308 | 7.69% | 9.231 s |
    | 15-min | 1 s | 0.99889 | 0.11% | 9.989 s |
    | 15-min | 2 s | 0.99778 | 0.22% | 9.978 s |
    | 15-min | 5 s | 0.99448 | 0.55% | 9.945 s |

    A forward step is the same arithmetic with the sign flipped — the file holds `nominal − delta` real seconds and gets stretched rather than squeezed. Scope of the damage is one file per adjustment: with a daily sync that is 1.04% of 15-minute files or 0.069% of 1-minute files, and realistic drift is 1–2 s, so the practical case is the top and fourth rows.

    Two consequences to accept deliberately rather than discover later. First, stored timestamps become **synthetic** — they no longer correspond to any clock reading that ever existed, so cross-referencing that file against another source (video overlay, an EVO secondary device) will not align exactly. Second, the time base becomes piecewise-warped: two events genuinely 1 s apart store 1.000 s apart in most files and 0.984 s apart in a corrected 1-minute file. Both are almost certainly fine for ATSPM measures, and both should be documented rather than silent.

  (a) is the safe default and is already the near-term plan. (c) looks like the right eventual answer — it buys correct ordering and near-correct durations for a bounded, computable cost, without the all-or-nothing commitment of (b). Prefer 15-minute files at any intersection where this correction will run, since the same `delta` costs roughly 15× less there. Do not start (b) without deciding it explicitly. Also note the marker pulse only covers adjustments *this tooling* makes — a keypad change at the cabinet or a power event still has to be inferred, and forward sets are close to undetectable from the data alone (naturally-occurring 5 s quiet gaps run 31–47 per hour, so no hole threshold can separate them).
- **Automatic video/data sync by detecting the signal head's color change.** `video-locate-phase-change` already automates everything *except* the actual detection: `first_phase_transition_after` (`analysis/video.py:421`) picks the earliest green→yellow / yellow→red edge for a phase out of `events`, the handler (`cli.py:1690`) converts it to an expected video-time offset, `extract_labeled_clip` (`video/processor.py:288`) renders a ±`--window` clip with a signed countdown burned into every frame, and passing the value back as `--observed-delta` prints the corrected `--start`. The human eye is the sensor: someone watches the clip, reads the label off the frame where the light visually changes, and re-runs. The feature is to replace that read with a pixel measurement and collapse the two calls into one.

  Shape of it: add a signal-head ROI to the shape config, sample its color per frame across the same search window the clip already covers, find the step, and feed the resulting offset into the existing correction arithmetic — which is already written and already tested. The plumbing that usually makes a CV feature expensive (frame decode, PTS-vs-FPS timing, indexless-`.ts` seeking, the sign convention on the delta) is all done and in use.

  Four things to decide rather than discover:
  - **Where the ROI comes from.** `_CSV_FIELDS` is `["type", "points", "color", "input", "phase", "name"]` and `type` is currently `loop`/`stopbar`; a third type (say `head`) carrying the phase it displays fits the existing schema and the calibration GUI's drawing machinery without a migration. Round-tripping through `ShapeConfig.load`/`.save` and the `relevant_phases()`/`relevant_overlaps()` filters (`data/video.py:210+`) needs checking — both currently branch on `type` explicitly.
  - **What to measure.** Hue is the obvious signal but is the fragile one: overexposed LED heads clip toward white and lose saturation, so a green→yellow edge can be a *saturation* step more than a hue step. Worth measuring on a real clip before committing — the answer may differ per camera, which argues for calibrating a per-head threshold at shape-drawing time rather than hardcoding one.
  - **Whether it degrades honestly.** Night glare, a head partly out of frame, rain on the lens, and a camera that moved since calibration all produce a plausible-looking wrong answer. Report a confidence (step magnitude vs. in-window noise) and fall back to the existing labeled clip when it's low, rather than silently emitting a bad `--start` — a wrong sync is worse than no sync, because every overlay downstream inherits it and looks fine.
  - **Sub-frame accuracy.** The DB edge is instantaneous; the video samples it at 1/fps, and the LED transition itself spans a frame or two on most cameras. So the achievable floor is roughly half a frame (~17 ms at 30 fps) plus whatever the head's own rise time is — fine for overlay alignment, worth stating so nobody chases precision the medium can't carry.

  Verification is cheap and non-speculative: clips already synced by hand have a known-good `--start`, so the detector can be scored against them directly.
- **Turning-movement counting via computer vision** (`EnhancedIOUTracker`, background-subtraction or YOLOv8 vehicle detection, approach-line crossing logic — carried over from the earlier `video_processing.py`). Deliberately split out of the Video Overlay work: model selection/tuning and tracking-accuracy validation is a substantially larger, different kind of problem. Revisit if there's appetite for it.
- **Intersection lane configuration (manual `int_cfg` schema & automated inference for Critical Movement Analysis).** Critical Movement Analysis (`src/atspm/analysis/critical.py`) currently computes `demand_per_lane = demand_vph / n_detectors`, using distinct stop-bar detector IDs (`Det_P{N}_Stopbar`) as a proxy for lane count. This proxy degrades when multiple detectors cover one lane (e.g. advance + stop-bar loops), when a single detector zone spans multiple lanes, or when shared lanes exist (e.g., shared Thru/Right or Thru/Left). Accurate Critical Movement Analysis and vphpl demand ratios require true lane counts and movement-to-lane mapping. Two complementary approaches to scope:

  - **Option 1: Explicit Manual Coding in `int_cfg.csv` (Deterministic / Configuration-based).**
    - Extend `int_cfg.csv` and the `config` table schema with explicit per-phase or per-movement lane configuration fields (e.g., `Lanes_P<N>` or `Lanes_TM_<Label>`).
    - Support shared-lane notation (e.g., `Shared_Lanes_P<N>_<M>`).
    - Update `phase_demand()` in `atspm.analysis.critical` to prefer configured lane counts over the `n_detectors` fallback when present.

  - **Option 2: Data-Driven Lane & Movement Inference (Detector & Flow Analysis).**
    - *Discharge Flow Rate / Saturation Capacity*: Compare observed maximum discharge rate during saturated green splits (`FlowRateEngine` / `atspm.analysis.flow`) against standard single-lane saturation flow (~1800–1900 vphpl) to infer effective number of discharge lanes per phase (e.g., ~3600 vph peak discharge → 2 lanes).
    - *Arrival / Departure Patterns & Shared Lanes*: Analyze vehicle-by-vehicle arrival patterns, co-actuations across movement detectors, and time-gap distributions during queue clearance to detect shared-lane behavior (e.g. left-turn blockages causing distinct headway distributions in shared thru/left lanes).
    - Surface inferred lane counts as an automated sanity check against `int_cfg.csv` configurations or as a fallback when configuration entries are omitted.

## Rejected / no action

Logged here so these don't get re-suggested or re-investigated later.

- **8 of 9 "Unused Import" suggestions were false positives** — checked every one against current usage; only `Literal` in `data/phases.py` was real (since removed). The rest (`argparse`/`sys` in `cli.py`, `List` in `reports/generators.py`, `Optional` in `utils/timezone.py`/`data/reader.py`, `Any`/`Dict` in `utils/logging.py`, `Dict` in `plotting/termination.py`, `pd` in `data/ingestion.py`/`data/detectors.py`) are all genuinely used. Likely stale from before recent edits.
- **`reports/__init__.py:22` "__all__ unused"** — references a class `ReportGenerator` that doesn't exist; the actual export is `PlotGenerator`. Stale/incorrect suggestion.
- **"Code Duplication in counts endpoints"** (`get_vehicle_counts`/`get_ped_counts`/`get_combined_counts`, `data/counts.py:595+`) — these are intentional thin wrappers matching the established Engine + `get_X` convenience-function pattern used everywhere else in `data/` (`AogEngine`/`get_arrival_on_green`, `PhaseEngine`/`get_phase_splits`, etc.). Collapsing them into one generic dispatcher would break consistency with their siblings for no real benefit — not recommended.
- **"Many Arguments" and "Overly Long" on `plot_coordination`** — both reference an old 11-argument signature that no longer exists. The function has already been refactored to 6 arguments and decomposed into ~15 helper functions (`_add_ring_bars`, `_add_coord_plan_markers`, `_add_detector_traces`, etc.). Stale — already addressed.
- **Notebook "Read stdout once Fix" / "List Files Fix"** (`notebooks/_Datz_SCP.ipynb`) — these already appear implemented, and are not part of the package itself (personal SCP automation). No action.
